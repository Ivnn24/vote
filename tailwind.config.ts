const config = {
  theme: {
    extend: {
      fontFamily: {
        sans: ['var(--font-poppins)'],
      },
      colors: {
        cetOrange: '#f05a28', // Based on your logo
      },
    },
  },
}